import 'package:flutter/material.dart';

class TablesView extends StatelessWidget {
  const TablesView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('TablesView'),
      ),
    );
  }
}
