import 'package:flutter/material.dart';

class PersonAddView extends StatelessWidget {
  const PersonAddView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('PersonAddView'),
      ),
    );
  }
}
