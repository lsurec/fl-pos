import 'package:flutter/material.dart';

class GarnishView extends StatelessWidget {
  const GarnishView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('GarnishView'),
      ),
    );
  }
}
