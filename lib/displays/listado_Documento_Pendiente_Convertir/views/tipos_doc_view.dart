import 'package:flutter/material.dart';

class TiposDocView extends StatelessWidget {
  const TiposDocView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('TiposDocView'),
      ),
    );
  }
}
